# Changelog

## [Unreleased]
- Initial project scaffold: public API headers, CMake build system, CI pipeline.
- CoreAudio backend stub (macOS/iOS).
- Universal test suite (constants, descriptors, math, handles).
